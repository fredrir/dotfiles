keypress_delay_ms: 20
keymap:
  - name: Konsole and kitty copy and interrupt
    application:
      only: /(?i)^(org\.kde\.konsole|kitty)$/
    exact_match: true
    remap:
      Ctrl-C: Ctrl-Shift-C
      Ctrl-X: Ctrl-C
