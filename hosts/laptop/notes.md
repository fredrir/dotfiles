# laptop

Arch Linux laptop, Hyprland only.

- GPU: integrated (no NVIDIA env).
- Display: eDP-1 1920x1080 @ 1.25 scale, with HiDPI Qt scaling tweaks in
  `hypr-local.conf`.

Install: `./setup.sh laptop`
