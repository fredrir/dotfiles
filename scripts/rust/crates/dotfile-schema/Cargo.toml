[package]
name = "dotfile-schema"
version = "0.1.0"
description = "Domain classification, typed HIR lowering, schema validation, bindings, and source maps for dotfile v1"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
dotfile-source = { path = "../dotfile-source" }
dotfile-syntax = { path = "../dotfile-syntax" }
serde_json.workspace = true
unicode-normalization.workspace = true
