[package]
name = "dotfile-source"
version = "0.1.0"
description = "Raw source, byte ranges, line indexes, diagnostics, and version bootstrap for dotfile v1"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
serde.workspace = true
serde_json.workspace = true
