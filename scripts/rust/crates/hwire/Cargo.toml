[package]
name              = "hwire"
version           = "0.1.0"
description       = "Latency and throughput between macie and archie"
edition.workspace = true
license.workspace = true

[dependencies]
clap.workspace        = true
serde_json.workspace  = true
workstation.workspace = true

# Binding the local address before connecting, and waiting on a socket with a
# timeout: std can do neither.
libc.workspace = true
