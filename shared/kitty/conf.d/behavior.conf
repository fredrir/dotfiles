mouse_hide_wait                0.0
scrollback_lines               100000
scrollback_pager_history_size  1000

# kitty's shell integration switches the cursor to a blinking bar at the prompt
# via zle-line-init. Turn that off and keep the block from cursor_shape.
shell_integration              enabled no-cursor
cursor_shape                   block
