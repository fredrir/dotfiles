# yazi keybindings

<!-- Generated by doc-keybinds; edit the source configuration. -->

[All packages](./_INDEX.md)

| Key | Context | Action | Description | Source |
| --- | --- | --- | --- | --- |
| <code>&lt;Esc&gt;</code> | <code>input; prepend_keymap</code> | <code>close</code> | Cancel input | [shared/yazi/keymap.toml:43](../../shared/yazi/keymap.toml#L43) |
| <code>&lt;Right&gt;</code> | <code>mgr; prepend_keymap</code> | <code>plugin smart-enter</code> | Enter directory or open file | [shared/yazi/keymap.toml:10](../../shared/yazi/keymap.toml#L10) |
| <code>M</code> | <code>mgr; prepend_keymap</code> | <code>plugin mount</code> | Open mount manager | [shared/yazi/keymap.toml:5](../../shared/yazi/keymap.toml#L5) |
| <code>f</code> | <code>mgr; prepend_keymap</code> | <code>plugin smart-filter</code> | Filter continuously and enter a unique directory | [shared/yazi/keymap.toml:23](../../shared/yazi/keymap.toml#L23) |
| <code>g → a</code> | <code>mgr; prepend_keymap</code> | <code>cd ~/dotfiles</code> | Go to ~/dotfiles | [shared/yazi/keymap.toml:33](../../shared/yazi/keymap.toml#L33) |
| <code>g → d</code> | <code>mgr; prepend_keymap</code> | <code>cd ~/Documents</code> | Go to ~/Documents | [shared/yazi/keymap.toml:38](../../shared/yazi/keymap.toml#L38) |
| <code>g → p</code> | <code>mgr; prepend_keymap</code> | <code>cd ~/projects</code> | Go to ~/projects | [shared/yazi/keymap.toml:28](../../shared/yazi/keymap.toml#L28) |
| <code>y</code> | <code>mgr; prepend_keymap</code> | <code>shell -- if [ -n "$WAYLAND_DISPLAY" ]; then for path in "$@"; do echo "file://$path"; done &#124; wl-copy -t text/uri-list; else printf "%s\n" "$@" &#124; dclip; fi; yank</code> | Yank and copy paths to the system clipboard | [shared/yazi/keymap.toml:15](../../shared/yazi/keymap.toml#L15) |
