# tmux keybindings

<!-- Generated by doc-keybinds; edit the source configuration. -->

[All packages](./_INDEX.md)

| Setting | Value | Context | Source |
| --- | --- | --- | --- |
| <code>mode-keys</code> | <code>vi</code> | — | [shared/tmux/20-copy.conf:1](../../shared/tmux/20-copy.conf#L1) |
| <code>prefix</code> | <code>C-b</code> | — | [shared/tmux/00-core.conf:33](../../shared/tmux/00-core.conf#L33) |
| <code>prefix2</code> | <code>None</code> | — | [shared/tmux/00-core.conf:34](../../shared/tmux/00-core.conf#L34) |
| <code>status-keys</code> | <code>emacs</code> | — | [shared/tmux/20-copy.conf:2](../../shared/tmux/20-copy.conf#L2) |
| <code>user-keys[0]</code> | <code>\e[13;2u</code> | — | [shared/tmux/00-core.conf:19](../../shared/tmux/00-core.conf#L19) |
| <code>user-keys[1]</code> | <code>\e[5;30012~</code> | — | [shared/tmux/00-core.conf:20](../../shared/tmux/00-core.conf#L20) |

| Key | Context | Action | Description | Source |
| --- | --- | --- | --- | --- |
| <code>/</code> | <code>copy-mode-vi</code> | <code>command-prompt -i -p 'Find' 'send-keys -X search-forward-incremental "%%"'</code> | Search forward incrementally | [shared/tmux/20-copy.conf:23](../../shared/tmux/20-copy.conf#L23) |
| <code>?</code> | <code>copy-mode-vi</code> | <code>command-prompt -i -p 'Find backward' 'send-keys -X search-backward-incremental "%%"'</code> | Search backward incrementally | [shared/tmux/20-copy.conf:24](../../shared/tmux/20-copy.conf#L24) |
| <code>C-a</code> | <code>copy-mode-vi</code> | <code>send-keys -X start-of-line</code> | Jump to beginning of line | [shared/tmux/20-copy.conf:19](../../shared/tmux/20-copy.conf#L19) |
| <code>C-e</code> | <code>copy-mode-vi</code> | <code>send-keys -X end-of-line</code> | Jump to end of line | [shared/tmux/20-copy.conf:20](../../shared/tmux/20-copy.conf#L20) |
| <code>C-v</code> | <code>copy-mode-vi</code> | <code>send-keys -X rectangle-toggle</code> | Toggle rectangular selection | [shared/tmux/20-copy.conf:11](../../shared/tmux/20-copy.conf#L11) |
| <code>DoubleClick1Pane</code> | <code>copy-mode-vi</code> | <code>select-pane \; send-keys -X select-word \; run-shell -d 0.3 \; send-keys -X copy-selection-no-clear</code> | Copy word and keep it highlighted | [shared/tmux/20-copy.conf:17](../../shared/tmux/20-copy.conf#L17) |
| <code>Enter</code> | <code>copy-mode-vi</code> | <code>send-keys -X copy-selection-and-cancel</code> | Copy selection and return to work | [shared/tmux/20-copy.conf:13](../../shared/tmux/20-copy.conf#L13) |
| <code>Escape</code> | <code>copy-mode-vi</code> | <code>send-keys -X cancel</code> | Leave copy mode | [shared/tmux/20-copy.conf:14](../../shared/tmux/20-copy.conf#L14) |
| <code>M-b</code> | <code>copy-mode-vi</code> | <code>send-keys -X previous-word</code> | Move backward a word | [shared/tmux/20-copy.conf:21](../../shared/tmux/20-copy.conf#L21) |
| <code>M-f</code> | <code>copy-mode-vi</code> | <code>send-keys -X next-word-end</code> | Move forward a word | [shared/tmux/20-copy.conf:22](../../shared/tmux/20-copy.conf#L22) |
| <code>MouseDragEnd1Pane</code> | <code>copy-mode-vi</code> | <code>send-keys -X copy-selection-no-clear</code> | Copy mouse selection and keep it highlighted | [shared/tmux/20-copy.conf:16](../../shared/tmux/20-copy.conf#L16) |
| <code>TripleClick1Pane</code> | <code>copy-mode-vi</code> | <code>select-pane \; send-keys -X select-line \; run-shell -d 0.3 \; send-keys -X copy-selection-no-clear</code> | Copy line and keep it highlighted | [shared/tmux/20-copy.conf:18](../../shared/tmux/20-copy.conf#L18) |
| <code>V</code> | <code>copy-mode-vi</code> | <code>send-keys -X select-line</code> | Select a complete line | [shared/tmux/20-copy.conf:10](../../shared/tmux/20-copy.conf#L10) |
| <code>q</code> | <code>copy-mode-vi</code> | <code>send-keys -X cancel</code> | Leave copy mode | [shared/tmux/20-copy.conf:15](../../shared/tmux/20-copy.conf#L15) |
| <code>v</code> | <code>copy-mode-vi</code> | <code>send-keys -X begin-selection</code> | Begin selection | [shared/tmux/20-copy.conf:9](../../shared/tmux/20-copy.conf#L9) |
| <code>y</code> | <code>copy-mode-vi</code> | <code>send-keys -X copy-selection-and-cancel</code> | Copy selection and return to work | [shared/tmux/20-copy.conf:12](../../shared/tmux/20-copy.conf#L12) |
| <code>[</code> | <code>copy-mode-vi; #{&gt;=:#{version},3.4}</code> | <code>send-keys -X previous-prompt</code> | Previous command prompt | [shared/tmux/20-copy.conf:30](../../shared/tmux/20-copy.conf#L30) |
| <code>]</code> | <code>copy-mode-vi; #{&gt;=:#{version},3.4}</code> | <code>send-keys -X next-prompt</code> | Next command prompt | [shared/tmux/20-copy.conf:31](../../shared/tmux/20-copy.conf#L31) |
| <code>{</code> | <code>copy-mode-vi; #{&gt;=:#{version},3.4}</code> | <code>send-keys -X previous-prompt -o</code> | Beginning of previous command output | [shared/tmux/20-copy.conf:32](../../shared/tmux/20-copy.conf#L32) |
| <code>}</code> | <code>copy-mode-vi; #{&gt;=:#{version},3.4}</code> | <code>send-keys -X next-prompt -o</code> | Beginning of next command output | [shared/tmux/20-copy.conf:33](../../shared/tmux/20-copy.conf#L33) |
| <code>"</code> | <code>prefix</code> | <code>Unbind</code> | Unbind | [shared/tmux/10-keys.conf:6](../../shared/tmux/10-keys.conf#L6) |
| <code>%</code> | <code>prefix</code> | <code>Unbind</code> | Unbind | [shared/tmux/10-keys.conf:7](../../shared/tmux/10-keys.conf#L7) |
| <code>&amp;</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" close-window --pane #{q:pane_id} --client #{q:client_name}'</code> | Close window with running-job protection | [shared/tmux/10-keys.conf:26](../../shared/tmux/10-keys.conf#L26) |
| <code>'</code> | <code>prefix</code> | <code>split-window -v -c '#{pane_current_path}'</code> | Split below in the current directory | [shared/tmux/10-keys.conf:10](../../shared/tmux/10-keys.conf#L10) |
| <code>*</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" scratch --pane #{q:pane_id} --client #{q:client_name}'</code> | Open a native floating scratch pane when supported | [shared/tmux/10-keys.conf:62](../../shared/tmux/10-keys.conf#L62) |
| <code>-</code> | <code>prefix</code> | <code>split-window -v -c '#{pane_current_path}'</code> | Split below in the current directory | [shared/tmux/10-keys.conf:11](../../shared/tmux/10-keys.conf#L11) |
| <code>.</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" shelf --pane #{q:pane_id} --client #{q:client_name}'</code> | Retrieve a pane from the shelf | [shared/tmux/10-keys.conf:60](../../shared/tmux/10-keys.conf#L60) |
| <code>0</code> | <code>prefix</code> | <code>select-window -t ':$'</code> | Last numbered window | [shared/tmux/10-keys.conf:54](../../shared/tmux/10-keys.conf#L54) |
| <code>;</code> | <code>prefix</code> | <code>last-pane</code> | Focus previous pane | [shared/tmux/10-keys.conf:18](../../shared/tmux/10-keys.conf#L18) |
| <code>?</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" help --pane #{q:pane_id} --client #{q:client_name}'</code> | Search bindings and their descriptions | [shared/tmux/10-keys.conf:58](../../shared/tmux/10-keys.conf#L58) |
| <code>A</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" handoff --pane #{q:pane_id} --client #{q:client_name}'</code> | Move agent execution to another machine | [shared/tmux/10-keys.conf:66](../../shared/tmux/10-keys.conf#L66) |
| <code>B</code> | <code>prefix</code> | <code>switch-client -T workspace-nested</code> | Forward keys to nested tmux; Ctrl-b Escape returns | [shared/tmux/10-keys.conf:76](../../shared/tmux/10-keys.conf#L76) |
| <code>C-b</code> | <code>prefix</code> | <code>send-prefix</code> | Forward prefix to the application or nested tmux | [shared/tmux/10-keys.conf:2](../../shared/tmux/10-keys.conf#L2) |
| <code>C-r</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" restore --pane #{q:pane_id} --client #{q:client_name}'</code> | Restore workspace recovery snapshot | [shared/tmux/10-keys.conf:72](../../shared/tmux/10-keys.conf#L72) |
| <code>C-s</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" save --pane #{q:pane_id} --client #{q:client_name}'</code> | Save workspace recovery snapshot | [shared/tmux/10-keys.conf:71](../../shared/tmux/10-keys.conf#L71) |
| <code>D</code> | <code>prefix</code> | <code>detach-client</code> | Detach this client; keep work running | [shared/tmux/10-keys.conf:4](../../shared/tmux/10-keys.conf#L4) |
| <code>Enter</code> | <code>prefix</code> | <code>copy-mode</code> | Enter copy mode and search scrollback | [shared/tmux/20-copy.conf:4](../../shared/tmux/20-copy.conf#L4) |
| <code>F7</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" host --pane #{q:pane_id} --client #{q:client_name}'</code> | Switch to a workspace on another host | [shared/tmux/10-keys.conf:67](../../shared/tmux/10-keys.conf#L67) |
| <code>I</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" inspect --pane #{q:pane_id} --client #{q:client_name}'</code> | Inspect key routing and terminal capabilities | [shared/tmux/10-keys.conf:70](../../shared/tmux/10-keys.conf#L70) |
| <code>M</code> | <code>prefix</code> | <code>swap-pane -t '{top-left}'</code> | Promote current pane to the first position | [shared/tmux/10-keys.conf:22](../../shared/tmux/10-keys.conf#L22) |
| <code>O</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" output --pane #{q:pane_id} --client #{q:client_name}'</code> | Search command output and deeper scrollback | [shared/tmux/10-keys.conf:69](../../shared/tmux/10-keys.conf#L69) |
| <code>R</code> | <code>prefix</code> | <code>switch-client -T workspace-resize</code> | Enter resize mode: hjkl adjust, Escape finishes | [shared/tmux/10-keys.conf:30](../../shared/tmux/10-keys.conf#L30) |
| <code>S</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" shelf-park --pane #{q:pane_id} --client #{q:client_name}'</code> | Park current pane on the shelf | [shared/tmux/10-keys.conf:59](../../shared/tmux/10-keys.conf#L59) |
| <code>Space</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" palette --pane #{q:pane_id} --client #{q:client_name}'</code> | Search workspace actions | [shared/tmux/10-keys.conf:57](../../shared/tmux/10-keys.conf#L57) |
| <code>Tab</code> | <code>prefix</code> | <code>last-window</code> | Last active window | [shared/tmux/10-keys.conf:53](../../shared/tmux/10-keys.conf#L53) |
| <code>Y</code> | <code>prefix</code> | <code>set-window-option synchronize-panes \; display-message 'Synchronized input: #{?synchronize-panes,ON,off}'</code> | Toggle synchronized input for this window | [shared/tmux/10-keys.conf:27](../../shared/tmux/10-keys.conf#L27) |
| <code>]</code> | <code>prefix</code> | <code>paste-buffer -p</code> | Paste the latest tmux buffer | [shared/tmux/20-copy.conf:5](../../shared/tmux/20-copy.conf#L5) |
| <code>&#96;</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" scratch --pane #{q:pane_id} --client #{q:client_name}'</code> | Toggle persistent scratch drawer | [shared/tmux/10-keys.conf:61](../../shared/tmux/10-keys.conf#L61) |
| <code>a</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" agent --pane #{q:pane_id} --client #{q:client_name}'</code> | Choose or resume an agent conversation | [shared/tmux/10-keys.conf:65](../../shared/tmux/10-keys.conf#L65) |
| <code>b</code> | <code>prefix</code> | <code>choose-buffer -Z</code> | Choose a saved clipboard buffer | [shared/tmux/20-copy.conf:6](../../shared/tmux/20-copy.conf#L6) |
| <code>c</code> | <code>prefix</code> | <code>new-window -c '#{pane_current_path}'</code> | New window in the current directory | [shared/tmux/10-keys.conf:50](../../shared/tmux/10-keys.conf#L50) |
| <code>d</code> | <code>prefix</code> | <code>split-window -h -c '#{pane_current_path}'</code> | Split right in the current directory | [shared/tmux/10-keys.conf:8](../../shared/tmux/10-keys.conf#L8) |
| <code>e</code> | <code>prefix</code> | <code>display-panes</code> | Choose a pane | [shared/tmux/10-keys.conf:19](../../shared/tmux/10-keys.conf#L19) |
| <code>f</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" quick-select --pane #{q:pane_id} --client #{q:client_name}'</code> | Quick-select a path, URL or token | [shared/tmux/10-keys.conf:68](../../shared/tmux/10-keys.conf#L68) |
| <code>g</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" lazygit --pane #{q:pane_id} --client #{q:client_name}'</code> | Open Lazygit for the current project | [shared/tmux/10-keys.conf:63](../../shared/tmux/10-keys.conf#L63) |
| <code>h</code> | <code>prefix</code> | <code>if-shell -F '#{@pane-is-vim}' 'send-keys C-h' 'select-pane -L'</code> | Focus left through editor splits and tmux panes | [shared/tmux/10-keys.conf:14](../../shared/tmux/10-keys.conf#L14) |
| <code>j</code> | <code>prefix</code> | <code>if-shell -F '#{@pane-is-vim}' 'send-keys C-j' 'select-pane -D'</code> | Focus below through editor splits and tmux panes | [shared/tmux/10-keys.conf:15](../../shared/tmux/10-keys.conf#L15) |
| <code>k</code> | <code>prefix</code> | <code>if-shell -F '#{@pane-is-vim}' 'send-keys C-k' 'select-pane -U'</code> | Focus above through editor splits and tmux panes | [shared/tmux/10-keys.conf:16](../../shared/tmux/10-keys.conf#L16) |
| <code>l</code> | <code>prefix</code> | <code>if-shell -F '#{@pane-is-vim}' 'send-keys C-l' 'select-pane -R'</code> | Focus right through editor splits and tmux panes | [shared/tmux/10-keys.conf:17](../../shared/tmux/10-keys.conf#L17) |
| <code>m</code> | <code>prefix</code> | <code>display-panes 'swap-pane -s "%%" -t "{top-left}"'</code> | Promote a chosen pane to the first position | [shared/tmux/10-keys.conf:21](../../shared/tmux/10-keys.conf#L21) |
| <code>n</code> | <code>prefix</code> | <code>next-window</code> | Next window | [shared/tmux/10-keys.conf:51](../../shared/tmux/10-keys.conf#L51) |
| <code>p</code> | <code>prefix</code> | <code>previous-window</code> | Previous window | [shared/tmux/10-keys.conf:52](../../shared/tmux/10-keys.conf#L52) |
| <code>q</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" close-pane --pane #{q:pane_id} --client #{q:client_name}'</code> | Close pane with running-job protection | [shared/tmux/10-keys.conf:23](../../shared/tmux/10-keys.conf#L23) |
| <code>r</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" reload --pane #{q:pane_id} --client #{q:client_name}'</code> | Reload and validate workspace configuration | [shared/tmux/10-keys.conf:3](../../shared/tmux/10-keys.conf#L3) |
| <code>s</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" projects --pane #{q:pane_id} --client #{q:client_name}'</code> | Find projects, worktrees and running sessions | [shared/tmux/10-keys.conf:56](../../shared/tmux/10-keys.conf#L56) |
| <code>t</code> | <code>prefix</code> | <code>new-window -c '#{pane_current_path}'</code> | New window in the current directory | [shared/tmux/10-keys.conf:49](../../shared/tmux/10-keys.conf#L49) |
| <code>w</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" close-window --pane #{q:pane_id} --client #{q:client_name}'</code> | Close window with running-job protection | [shared/tmux/10-keys.conf:24](../../shared/tmux/10-keys.conf#L24) |
| <code>x</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" close-pane --pane #{q:pane_id} --client #{q:client_name}'</code> | Close pane with running-job protection | [shared/tmux/10-keys.conf:25](../../shared/tmux/10-keys.conf#L25) |
| <code>y</code> | <code>prefix</code> | <code>run-shell -b '"#{@workspace_config}/bin/tmux-workspace" yazi --pane #{q:pane_id} --client #{q:client_name}'</code> | Open Yazi for the current directory | [shared/tmux/10-keys.conf:64](../../shared/tmux/10-keys.conf#L64) |
| <code>z</code> | <code>prefix</code> | <code>resize-pane -Z</code> | Zoom or unzoom current pane | [shared/tmux/10-keys.conf:20](../../shared/tmux/10-keys.conf#L20) |
| <code>&#124;</code> | <code>prefix</code> | <code>split-window -h -c '#{pane_current_path}'</code> | Split right in the current directory | [shared/tmux/10-keys.conf:9](../../shared/tmux/10-keys.conf#L9) |
| <code>Down</code> | <code>prefix; #{&gt;=:#{version},3.4}</code> | <code>copy-mode \; send-keys -X next-prompt</code> | Jump to next command prompt | [shared/tmux/20-copy.conf:29](../../shared/tmux/20-copy.conf#L29) |
| <code>Up</code> | <code>prefix; #{&gt;=:#{version},3.4}</code> | <code>copy-mode \; send-keys -X previous-prompt</code> | Jump to previous command prompt | [shared/tmux/20-copy.conf:28](../../shared/tmux/20-copy.conf#L28) |
| <code>Down</code> | <code>prefix; not (#{&gt;=:#{version},3.4})</code> | <code>copy-mode \; send-keys -X history-bottom</code> | Return to the bottom of scrollback (tmux 3.3) | [shared/tmux/20-copy.conf:36](../../shared/tmux/20-copy.conf#L36) |
| <code>Up</code> | <code>prefix; not (#{&gt;=:#{version},3.4})</code> | <code>copy-mode \; send-keys -X history-top</code> | Search previous command in scrollback (tmux 3.3) | [shared/tmux/20-copy.conf:35](../../shared/tmux/20-copy.conf#L35) |
| <code>H</code> | <code>prefix; repeat</code> | <code>resize-pane -L 3</code> | Resize pane left | [shared/tmux/10-keys.conf:31](../../shared/tmux/10-keys.conf#L31) |
| <code>J</code> | <code>prefix; repeat</code> | <code>resize-pane -D 3</code> | Resize pane down | [shared/tmux/10-keys.conf:32](../../shared/tmux/10-keys.conf#L32) |
| <code>K</code> | <code>prefix; repeat</code> | <code>resize-pane -U 3</code> | Resize pane up | [shared/tmux/10-keys.conf:33](../../shared/tmux/10-keys.conf#L33) |
| <code>L</code> | <code>prefix; repeat</code> | <code>resize-pane -R 3</code> | Resize pane right | [shared/tmux/10-keys.conf:34](../../shared/tmux/10-keys.conf#L34) |
| <code>User0</code> | <code>root</code> | <code>send-keys -l "\e[13;2u"</code> | Pass Shift+Enter to the shell or application | [shared/tmux/00-core.conf:21](../../shared/tmux/00-core.conf#L21) |
| <code>User1</code> | <code>root</code> | <code>send-keys -l "\e[115;9u"</code> | Pass the Yazi shell widget sequence | [shared/tmux/00-core.conf:24](../../shared/tmux/00-core.conf#L24) |
| <code>Any</code> | <code>workspace-nested</code> | <code>send-keys \; switch-client -T workspace-nested</code> | Send keys | [shared/tmux/10-keys.conf:78](../../shared/tmux/10-keys.conf#L78) |
| <code>C-b</code> | <code>workspace-nested</code> | <code>switch-client -T workspace-nested-prefix</code> | Switch client | [shared/tmux/10-keys.conf:77](../../shared/tmux/10-keys.conf#L77) |
| <code>Any</code> | <code>workspace-nested-prefix</code> | <code>send-prefix \; send-keys \; switch-client -T workspace-nested</code> | Send prefix | [shared/tmux/10-keys.conf:80](../../shared/tmux/10-keys.conf#L80) |
| <code>Escape</code> | <code>workspace-nested-prefix</code> | <code>switch-client -T root</code> | Switch client | [shared/tmux/10-keys.conf:79](../../shared/tmux/10-keys.conf#L79) |
| <code>=</code> | <code>workspace-resize</code> | <code>select-layout -E \; switch-client -T workspace-resize</code> | Balance pane sizes | [shared/tmux/10-keys.conf:43](../../shared/tmux/10-keys.conf#L43) |
| <code>Any</code> | <code>workspace-resize</code> | <code>switch-client -T root \; send-keys</code> | Switch client | [shared/tmux/10-keys.conf:47](../../shared/tmux/10-keys.conf#L47) |
| <code>Enter</code> | <code>workspace-resize</code> | <code>switch-client -T root</code> | Finish resizing | [shared/tmux/10-keys.conf:46](../../shared/tmux/10-keys.conf#L46) |
| <code>Escape</code> | <code>workspace-resize</code> | <code>switch-client -T root</code> | Finish resizing | [shared/tmux/10-keys.conf:45](../../shared/tmux/10-keys.conf#L45) |
| <code>H</code> | <code>workspace-resize</code> | <code>resize-pane -L 8 \; switch-client -T workspace-resize</code> | Resize left quickly | [shared/tmux/10-keys.conf:39](../../shared/tmux/10-keys.conf#L39) |
| <code>J</code> | <code>workspace-resize</code> | <code>resize-pane -D 8 \; switch-client -T workspace-resize</code> | Resize down quickly | [shared/tmux/10-keys.conf:40](../../shared/tmux/10-keys.conf#L40) |
| <code>K</code> | <code>workspace-resize</code> | <code>resize-pane -U 8 \; switch-client -T workspace-resize</code> | Resize up quickly | [shared/tmux/10-keys.conf:41](../../shared/tmux/10-keys.conf#L41) |
| <code>L</code> | <code>workspace-resize</code> | <code>resize-pane -R 8 \; switch-client -T workspace-resize</code> | Resize right quickly | [shared/tmux/10-keys.conf:42](../../shared/tmux/10-keys.conf#L42) |
| <code>Space</code> | <code>workspace-resize</code> | <code>next-layout \; switch-client -T workspace-resize</code> | Cycle layout | [shared/tmux/10-keys.conf:44](../../shared/tmux/10-keys.conf#L44) |
| <code>h</code> | <code>workspace-resize</code> | <code>resize-pane -L 2 \; switch-client -T workspace-resize</code> | Resize left | [shared/tmux/10-keys.conf:35](../../shared/tmux/10-keys.conf#L35) |
| <code>j</code> | <code>workspace-resize</code> | <code>resize-pane -D 2 \; switch-client -T workspace-resize</code> | Resize down | [shared/tmux/10-keys.conf:36](../../shared/tmux/10-keys.conf#L36) |
| <code>k</code> | <code>workspace-resize</code> | <code>resize-pane -U 2 \; switch-client -T workspace-resize</code> | Resize up | [shared/tmux/10-keys.conf:37](../../shared/tmux/10-keys.conf#L37) |
| <code>l</code> | <code>workspace-resize</code> | <code>resize-pane -R 2 \; switch-client -T workspace-resize</code> | Resize right | [shared/tmux/10-keys.conf:38](../../shared/tmux/10-keys.conf#L38) |
